# bat の設定 + カスタムテーマ（themes/ayu-dark.tmTheme）。
# 設計: docs/dotfiles-native-design.md §6
#
# サブディレクトリ themes/ も相対構造を保って再帰配置される（ディレクトリ単位 copy）。
# kind は省略時 "copy"。bat cache の再構築（themes 登録）は hooks スライスで対応する。
dst = "~/.config/bat"
