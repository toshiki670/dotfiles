# bat の fish 補完を生成・配置する。
# when.deps = bat が PATH に無ければ生成をスキップ（ユニット gate）。
when = { deps = [ "bat" ] }

[[steps]]
input.cmd = [ "bat", "--completion", "fish" ]

[[steps]]
output = "~/.config/fish/completions/bat.fish"
