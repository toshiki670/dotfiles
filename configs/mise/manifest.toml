# mise 自身の managed 設定を配置する。
#
# 配置先は ~/.config/mise（ディレクトリ）。配下の config.toml だけを管理し、user 所有の
# ~/.config/mise/conf.d/*.toml（マシン固有の任意設定）には一切干渉しない。

[[steps]]
input = "."

[[steps]]
output = "~/.config/mise"
