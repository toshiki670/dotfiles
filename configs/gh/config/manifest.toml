# gh 本体設定（config.yml）を ~/.config/gh へ copy する。
#
# git_protocol / editor / prompt / pager 等、gh 自身が読む設定一式。completion（fish 補完を生成する別単位）は
# 同じ configs/gh 配下でも別単位（configs/gh/completion）として分離済み。
#

[[steps]]
input = "."

[[steps]]
output = "~/.config/gh"
