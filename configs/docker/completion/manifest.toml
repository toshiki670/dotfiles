# docker の fish 補完を生成・配置する。
# when.deps = docker が PATH に無ければ生成をスキップ（ユニット gate）。
when = { deps = [ "docker" ] }

[[steps]]
input.cmd = [ "docker", "completion", "fish" ]

[[steps]]
output = "~/.config/fish/completions/docker.fish"
