# docker の fish 補完を生成・配置する（generate 層）。
# `docker completion fish` の出力を dst のファイルへ書き出す。
# when.deps = docker が PATH に無ければ生成をスキップ（ユニット gate）。
cmd  = [ "docker", "completion", "fish" ]
dst  = "~/.config/fish/completions/docker.fish"
kind = "generate"
when = { deps = [ "docker" ] }
