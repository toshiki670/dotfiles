# merge-ready（外部ツール）の fish 補完を生成・配置する。
# when.deps = merge-ready が PATH に無ければ生成をスキップ（ユニット gate）。
when = { deps = [ "merge-ready" ] }

[[steps]]
input.cmd = [ "merge-ready", "completions", "fish" ]

[[steps]]
output = "~/.config/fish/completions/merge-ready.fish"
