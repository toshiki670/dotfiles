# ログインシェルとインタラクティブシェルの場合だけ読み込まれる。
# シェルスクリプトでは不要な場合に記述する。

# DOTFILES変数の設定（chezmoiのsourceDirを使用）
export DOTFILES="{{ .chezmoi.sourceDir }}"

# Setting completions
{{ includeTemplate "dot_config/zsh/configs/docker.zsh" }}

# History config
{{ includeTemplate "dot_config/zsh/configs/history.zsh" }}

# Initialize and Load Sheldon (must be before completion.zsh)
{{ includeTemplate "dot_config/zsh/configs/sheldon.zsh.tmpl" }}

# fzf config (after sheldon to ensure fzf is loaded)
{{ includeTemplate "dot_config/zsh/configs/fzf.zsh" }}

# fzf-tab config (after fzf and before completion.zsh)
{{ includeTemplate "dot_config/zsh/configs/fzf-tab.zsh" }}

# Completion config (after sheldon to detect all completion plugins)
{{ includeTemplate "dot_config/zsh/configs/completion.zsh" }}

# eza command config
{{ includeTemplate "dot_config/zsh/configs/eza.zsh" }}

# cd config
{{ includeTemplate "dot_config/zsh/configs/cd.zsh" }}

# zoxide config
{{ includeTemplate "dot_config/zsh/configs/zoxide.zsh.tmpl" }}

# ruby and rails config
{{ includeTemplate "dot_config/zsh/configs/ruby.zsh" }}

# python config
{{ includeTemplate "dot_config/zsh/configs/python.zsh" }}

# C language config
{{ includeTemplate "dot_config/zsh/configs/c.zsh" }}

# git config
{{ includeTemplate "dot_config/zsh/configs/git.zsh" }}

# dotfiles version management config
{{ includeTemplate "dot_config/zsh/configs/dotfiles.zsh" }}

# vim config
{{ includeTemplate "dot_config/zsh/configs/vim.zsh" }}

# pacman config (only if pacman is installed)
{{ if lookPath "pacman" -}}
{{ includeTemplate "dot_config/zsh/configs/pacman.zsh" }}
{{ end -}}

# mise config
{{ includeTemplate "dot_config/zsh/configs/mise.zsh.tmpl" }}

# yt-dlp config (only if YT_BROWSER is set)
{{ if env "YT_BROWSER" -}}
{{ includeTemplate "dot_config/zsh/configs/yt-dlp.zsh" }}
{{ end -}}

# Homebrew config - Must be loaded AFTER all plugins (sheldon)
{{ includeTemplate "dot_config/zsh/configs/brew.zsh" }}

# Common config
{{ includeTemplate "dot_config/zsh/configs/common.zsh" }}

# ターミナル起動時に実行

# Daily package check
{{ includeTemplate "dot_config/zsh/configs/daily-check.zsh" }}

# ZSHの起動した関数の時間計測 .zshenv参照
# zprof
