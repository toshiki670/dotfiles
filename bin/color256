#!/usr/bin/env python3
#
# 256色のカラーパレットを表示する
#

RESET = "\033[0m"

for code in range(256):
    print(f"\033[48;5;{code}m\033[38;5;0m {code:03d} \033[0m", end="")
    if (code + 1) % 16 == 0:
        print(RESET)
print(RESET, end="")
