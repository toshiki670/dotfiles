#!/bin/bash

usage=$(cat << __EOF__
  Usage:  completions [OPTIONS] COMMAND
  
  Options:
    -i
    -h, --help          Display this help and quit
    -v, --version       Print version infomation and quit
  
__EOF__
)



echo "$usage"
